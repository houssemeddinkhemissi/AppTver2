<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.khemissi.houssemeddin.apptver2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AjoutChaineActivity" />
        <activity android:name=".AfficherHtmlEmissActivity" />
        <activity android:name=".AfficherXmlEmissActivity" />
        <activity android:name=".AffChaineActivity" />
        <activity android:name=".LoginActivity" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

        <activity android:name=".AfficherPaysActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".StreamActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".AfficherEmissionActivity" />
        <activity android:name=".ListEmissionsActivity" />
        <activity android:name=".AfficherEmissActivity">

        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ProfilActivity"></activity>
    </application>

</manifest>